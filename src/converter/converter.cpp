#include "../../include/converter/converter.h"

using namespace SIGA;
using namespace SIGA::DS;

tree<TreeNode> SIGA::Converter::loadTree(const std::string &filename,
                                         const ParserOptions &parserOptions,
                                         const ConverterOptions &converterOptions)
{
    tree<TreeNode> result;
    return result;
}

void Converter::load(const char *filename, const ParserOptions& options,
                     const ConverterOptions& cOptions)
{

}
